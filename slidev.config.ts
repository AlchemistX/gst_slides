import { defineConfig } from '@slidev/cli'

export default defineConfig({
  css: 'unocss',
  highlighter: 'shiki',
  lineNumbers: false,
  monaco: true,
  remoteAssets: false,
  selectable: false,
  theme: 'default',
  themeConfig: {},
})
